import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'final_isa_angular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
